server:
  port: 8010
spring:
  # 当前服务注册真心爱Eureka Server的名称
  application:
    name: provider
eureka:
  client:
    service-url:
      # 注册中心的访问地址
      defaultZone: http://localhost:8761/eureka/
  instance:
    # 是否将当前服务的ip注册到 Eureka Server
    prefer-ip-address: true